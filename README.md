# Distributed Lock Manager

Production-ready distributed lock manager built with Node.js, Express, MongoDB, React, Docker, PM2, deadlock detection, metrics dashboard, monitoring scripts, and lock expiration workers.

## Local Dev
- Backend: `cd backend && npm install && npm run dev`
- Frontend: `cd frontend && npm install && npm run dev`

## Docker Deploy
- `./scripts/deploy.ps1`